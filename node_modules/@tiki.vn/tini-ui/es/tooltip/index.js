import fmtEvent from '../_util/fmtEvent';
Component({
  props: {
    className: '',
    style: '',
    show: false,
    theme: 'default',
    content: '',
    position: 'top',
    iconSizeClose: 16,
    showCloseIcon: false,
    onClose: function onClose() {}
  },
  methods: {
    _onClose: function _onClose(e) {
      var onClose = this.props.onClose;

      if (typeof onClose === 'function') {
        var _event = fmtEvent(this.props, e);

        onClose(_event);
      }
    }
  }
});