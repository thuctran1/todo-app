Component({
  data: {
    isShowRemoveIcon: false
  },
  props: {
    className: '',
    style: '',
    mode: 'aspectFill',
    showPreview: true,
    image: {
      path: ''
    }
  },
  methods: {
    handleTapImage: function handleTapImage() {
      var _this$props = this.props,
          showPreview = _this$props.showPreview,
          image = _this$props.image;
      if (!showPreview) return;
      my.previewImage({
        urls: [image.path]
      });
    },
    handleRemoveImage: function handleRemoveImage() {
      this.props.onRemoveImage(this.props.image);
    }
  },
  didMount: function didMount() {
    this.setData({
      isShowRemoveIcon: typeof this.props.onRemoveImage === 'function'
    });
  }
});
export {};