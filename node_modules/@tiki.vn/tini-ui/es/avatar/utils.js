var colors = ['blue30', 'green30', 'orange30', 'red30', 'yellow30'];
export var getShortname = function getShortname(name) {
  try {
    if (!name || !("" + name).trim().length) {
      return '';
    }

    var split = name.trim().split(' ');
    var rs = (split[split.length - 2] || '').charAt(0) + split[split.length - 1].charAt(0);
    return rs.toUpperCase();
  } catch (_unused) {
    return '';
  }
};
export var getColorName = function getColorName(name) {
  try {
    if (!name || ("" + name).trim().length === 0) {
      return colors[0];
    }

    var hash = 0;

    for (var i = 0; i < name.length; i++) {
      hash = name.charCodeAt(i) + ((hash << 5) - hash);
      hash = hash & hash;
    }

    hash = (hash % colors.length + colors.length) % colors.length;
    return colors[hash];
  } catch (_unused2) {
    return colors[0];
  }
};
export var getFontSize = function getFontSize(size) {
  if (size > 24 && size <= 40) {
    return 14;
  } else if (size > 40 && size <= 56) {
    return 18;
  } else if (size > 56) {
    return 24;
  }

  return 10;
};