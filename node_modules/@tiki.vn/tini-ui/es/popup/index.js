Component({
  props: {
    className: '',
    show: false,
    position: 'bottom',
    mask: true,
    maskClose: true,
    animation: true,
    disableScroll: true
  },
  methods: {
    onMaskTap: function onMaskTap() {
      var _this$props = this.props,
          maskClose = _this$props.maskClose,
          onClose = _this$props.onClose,
          animation = _this$props.animation;

      if (maskClose && onClose) {
        if (animation) {
          onClose();
        } else {
          setTimeout(function () {
            onClose();
          }, 200);
        }
      }
    }
  }
});