/* eslint-disable */
var jsUnitRpx = 'false';
/* eslint-disable no-continue, prefer-spread */

export default function fmtUnit(oldUnit) {
  var getUnit = oldUnit;

  if (jsUnitRpx) {
    if (typeof getUnit === 'string' && getUnit === 'px') {
      getUnit = 'rpx';
    } else if (typeof getUnit === 'number') {
      getUnit = (parseInt(getUnit) * 2).toString();
    } else if (typeof getUnit === 'string') {
      var matches = oldUnit.match(/(\d+|\d+\.\d+)(px)/);
      var number = Array.isArray(matches) && matches.length ? matches[1] : getUnit;
      getUnit = parseInt(number) * 2 + 'rpx';
    }
  }

  return getUnit;
}