export var isHasValue = function isHasValue(value) {
  return value !== null && value !== undefined;
};
export var isNotEmpty = function isNotEmpty(value) {
  return isHasValue(value) && ("" + value).trim().length > 0;
};